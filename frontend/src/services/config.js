//const dev_url = "http://localhost:5000/api";
const prod_url = "https://cfm-portal.netlify.app/api";

const config = {
  baseUrl: prod_url,
  //token: StorageService.getStringData("access_token"),
};

export default config;
